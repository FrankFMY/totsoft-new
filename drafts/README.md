# Totsoft Landing Page

Современный корпоративный лендинг для IT-компании Тотсофт. Разработка высоконагруженных IT-решений Государства и Бизнеса.

## 👨‍💻 Автор

**Артём Прянишников**

- GitHub: [FrankFMY](https://github.com/FrankFMY)
- Telegram: [@FrankFMY](https://t.me/FrankFMY)

## 🚀 Технологии

- **React 19** - современный UI фреймворк
- **TypeScript** - строгая типизация
- **Vite** - быстрая сборка и разработка
- **Tailwind CSS v4** - utility-first CSS фреймворк
- **Biome** - линтер и форматтер кода (замена ESLint + Prettier)
- **Lucide React** - иконки
- **React Router** - маршрутизация для SPA
- **Vercel Serverless Functions** - backend для отправки писем
- **Nodemailer** - отправка писем через SMTP (Beget)

## 📋 Требования

- Node.js 18+
- pnpm (рекомендуется) или npm/yarn

## 🛠️ Установка

1. Клонируйте репозиторий:

```bash
git clone <repository-url>
cd Totsoft-Landing
```

2. Установите зависимости:

```bash
pnpm install
```

3. (Обязательно для продакшена) Создайте файл `.env.local` в корне проекта:

```env
# SMTP настройки для Beget (используются Vercel Serverless Function)
SMTP_USER=dev@totsoft.net
SMTP_PASSWORD=ваш_пароль_от_почты
```

**Важно:** После деплоя на Vercel добавьте переменные окружения в настройках проекта:
Settings -> Environment Variables -> Add:

- `SMTP_USER` = `dev@totsoft.net`
- `SMTP_PASSWORD` = ваш пароль от почты

**Примечание:** Для локальной разработки форма будет использовать симуляцию отправки (если не запущен Vercel CLI).

## 🏃 Запуск

### Режим разработки

```bash
pnpm dev
```

Приложение будет доступно по адресу `http://localhost:3000`

### Сборка для продакшена

```bash
pnpm build
```

### Превью продакшен сборки

```bash
pnpm preview
```

### Полная проверка и сборка

```bash
pnpm full
```

Запускает полный цикл проверки: форматирование, линтинг, проверка типов и сборка проекта.

## 📁 Структура проекта

```
Totsoft-Landing/
├── components/          # React компоненты
│   ├── About.tsx       # Секция "О нас"
│   ├── ContactForm.tsx # Форма обратной связи
│   ├── TrustBar.tsx    # Блок доверия с ключевыми фактами
│   ├── ErrorBoundary.tsx # Обработка ошибок
│   └── PrivacyPolicy.tsx # Страница политики конфиденциальности
│   ├── Footer.tsx      # Футер
│   ├── Hero.tsx        # Главная секция
│   ├── Navbar.tsx      # Навигация
│   ├── Process.tsx     # Процесс работы
│   ├── Services.tsx    # Услуги
│   └── TechStack.tsx   # Технологический стек
├── config/             # Конфигурация
│   └── constants.ts    # Константы приложения
├── api/                # Vercel Serverless Functions
│   └── send-email.ts   # Функция отправки писем через Beget SMTP
├── api/                # Vercel Serverless Functions
│   └── send-email.ts   # Функция отправки писем через Beget SMTP (для Vercel)
├── server/             # Backend сервер (для деплоя на собственный сервер)
│   ├── index.js        # Express сервер для отправки писем
│   ├── package.json    # Зависимости backend
│   └── Dockerfile      # Dockerfile для backend
├── services/           # Сервисы
│   ├── api.ts          # API для отправки форм
│   └── geminiService.ts # AI сервис (если используется)
├── public/             # Статические файлы
│   ├── robots.txt      # Файл для поисковых роботов
│   └── sitemap.xml     # Карта сайта
├── utils/              # Утилиты
│   ├── debounce.ts     # Debounce функция
│   └── validation.ts   # Валидация форм
├── App.tsx             # Главный компонент
├── index.tsx           # Точка входа
├── index.html          # HTML шаблон
├── index.css           # Глобальные стили
├── tailwind.config.js  # Конфигурация Tailwind
├── postcss.config.js   # Конфигурация PostCSS
├── tsconfig.json       # Конфигурация TypeScript
└── vite.config.ts      # Конфигурация Vite
```

## 🔧 Конфигурация

### Переменные окружения

**Для Vercel:**

- `SMTP_USER` - Email для отправки писем (dev@totsoft.net)
- `SMTP_PASSWORD` - Пароль от почты для SMTP

**Для собственного сервера (Docker):**

- `SMTP_USER` - Email для отправки писем (dev@totsoft.net)
- `SMTP_PASSWORD` - Пароль от почты для SMTP
- `VITE_API_URL` - URL backend API (по умолчанию `/api/send-email`, nginx проксирует на backend)

Эти переменные используются:

- На Vercel: Vercel Serverless Function (`api/send-email.ts`)
- На собственном сервере: Express backend (`server/index.js`)

### TypeScript

Проект использует строгий режим TypeScript (`strict: true`) для максимальной типобезопасности.

### Tailwind CSS

Конфигурация Tailwind находится в `tailwind.config.js`. Кастомные цвета и анимации определены там.

## 🎨 Особенности

- ✅ **Валидация форм** - полная валидация всех полей с отображением ошибок и согласием на обработку данных
- ✅ **Lazy loading** - оптимизация загрузки компонентов
- ✅ **SEO оптимизация** - мета-теги, Open Graph, структурированные данные, robots.txt, sitemap.xml
- ✅ **Accessibility** - поддержка ARIA атрибутов, семантической разметки, skip links
- ✅ **Адаптивный дизайн** - корректное отображение на всех устройствах
- ✅ **TypeScript strict mode** - строгая типизация для надежности кода
- ✅ **Error Boundary** - обработка ошибок рендеринга
- ✅ **Роутинг** - React Router для навигации между страницами

## 🔒 Безопасность

✅ **Безопасность реализована через Vercel Serverless Functions:**

1. Все секреты (SMTP пароли) хранятся только в переменных окружения Vercel
2. SMTP настройки недоступны в браузерном коде
3. Валидация данных выполняется на стороне сервера
4. Письма отправляются через защищенное SMTP соединение (SSL/TLS)

## 📝 API

### Отправка формы контакта

Форма отправляет данные через Vercel Serverless Function (`/api/send-email`), которая отправляет письмо на `dev@totsoft.net` через Beget SMTP.

```typescript
import { submitContactForm } from './services/api';

const result = await submitContactForm({
	name: 'Иван Иванов',
	email: 'ivan@example.com',
	phone: '+7 (999) 123-45-67',
	description: 'Описание проекта...'
});

if (result.success) {
	console.log('Заявка отправлена!');
} else {
	console.error('Ошибка:', result.error);
}
```

## 🧪 Разработка

### Линтинг и форматирование

Проект использует **Biome** для линтинга и форматирования кода:

```bash
# Полная проверка и сборка (форматирование + линтинг + проверка типов + сборка)
pnpm full

# Проверить код на ошибки и форматирование
pnpm check

# Автоматически исправить все проблемы
pnpm check:fix

# Только линтинг
pnpm lint

# Только форматирование
pnpm format

# Проверка типов TypeScript
pnpm typecheck
```

### Добавление нового компонента

1. Создайте файл в `components/`
2. Используйте TypeScript и строгую типизацию
3. Следуйте существующим паттернам проекта
4. Запустите `pnpm check:fix` перед коммитом

### Валидация

Используйте утилиты из `utils/validation.ts`:

```typescript
import { validateEmail, validatePhone } from '../utils/validation';

const emailResult = validateEmail(email);
if (!emailResult.isValid) {
	console.error(emailResult.message);
}
```

## 🔄 CI/CD

Проект использует **GitHub Actions** для автоматической проверки кода при каждом push и pull request.

Workflow выполняет следующие шаги в указанном порядке:

1. Форматирование кода (`pnpm format`)
2. Линтинг и исправление ошибок (`pnpm lint:fix`)
3. Проверка и исправление (`pnpm check:fix`)
4. Проверка типов TypeScript (`pnpm typecheck`)
5. Сборка проекта (`pnpm build`)

Файл конфигурации: `.github/workflows/ci.yml`

CI запускается автоматически при:

- Push в ветки `main` и `develop`
- Создании Pull Request в ветки `main` и `develop`

## 📦 Деплой

### Vercel (рекомендуется)

```bash
vercel
```

После деплоя добавьте переменные окружения в настройках проекта:

- `SMTP_USER` = `dev@totsoft.net`
- `SMTP_PASSWORD` = ваш пароль

### Docker (для деплоя на собственный сервер)

Проект включает backend сервер (Express) для отправки писем через Beget SMTP.

**Сборка и запуск через docker-compose (рекомендуется):**

```bash
# Создайте .env файл в корне проекта с настройками SMTP:
# SMTP_USER=dev@totsoft.net
# SMTP_PASSWORD=ваш_пароль

# Запуск всех сервисов (frontend + backend)
docker-compose up -d
```

**Или вручную:**

```bash
# Backend
cd server
docker build -t totsoft-backend .
docker run -d -p 3001:3001 \
  -e SMTP_USER=dev@totsoft.net \
  -e SMTP_PASSWORD=ваш_пароль \
  --name totsoft-backend totsoft-backend

# Frontend
docker build -t totsoft-frontend --build-arg VITE_API_URL=/api/send-email .
docker run -d -p 80:80 --name totsoft-frontend totsoft-frontend
```

**Архитектура:**

- **Frontend** (nginx) - порт 80, отдает статику и проксирует `/api/*` на backend
- **Backend** (Express) - порт 3001, обрабатывает отправку писем

**Настройка DNS:**

После деплоя на сервер добавьте A-запись в DNS:

- `totsoft.net` → IP_СЕРВЕРА

**Переменные окружения для backend:**

- `SMTP_USER` - email для отправки (dev@totsoft.net)
- `SMTP_PASSWORD` - пароль от почты
- `PORT` - порт backend сервера (по умолчанию 3001)

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для фичи (`git checkout -b feature/AmazingFeature`)
3. Закоммитьте изменения (`git commit -m 'Add some AmazingFeature'`)
4. Запушьте в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Проект является собственностью ООО "Тотсофт". См. файл [LICENSE](LICENSE) для подробностей.

## 📞 Контакты

- Email: dev@totsoft.net
- Website: https://totsoft.net

---

Разработано с ❤️ [Артёмом Прянишниковым](https://github.com/FrankFMY) для команды Тотсофт
