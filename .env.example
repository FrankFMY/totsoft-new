# Frontend environment variables (SvelteKit)
# Скопируйте этот файл в .env и заполните значения

# SMTP настройки для отправки email через SvelteKit API route
# Если не указаны, форма будет работать в режиме симуляции (dev режим)
SMTP_USER=dev@totsoft.net
SMTP_PASSWORD=ваш_пароль_от_почты

